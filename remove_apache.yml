---
- name: Ansible Tutorial
  hosts: all
  become: true
  tasks:
    - name: Remove apache2 and php packages for ubuntu
      package:
        name:
          - "{{ apache_package }}"
          - "{{ php_package }}"
        state: absent
        update_cache: true
